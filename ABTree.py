# This algorithm will use the information from our knowledge base about the current play session
# to asses which moves are most effective. The current evaluation function will be something simple:
# Our HP/their HP

def assessUtility(defender, aggressor):


def minmax(node, depth, isMax, alpha, beta):
